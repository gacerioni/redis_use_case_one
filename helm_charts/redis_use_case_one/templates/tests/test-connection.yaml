apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "redis_use_case_one.fullname" . }}-test-connection"
  labels:
    {{- include "redis_use_case_one.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "redis_use_case_one.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
